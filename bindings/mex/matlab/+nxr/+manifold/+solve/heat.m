function out = heat(mctx, sources, sourceValues, timesteps, alpha) %#ok<INUSD>
%HEAT  Spectral heat diffusion over a list of timesteps.
%   nxr.manifold.solve.heat(mctx, sources, sourceValues, timesteps, alpha)
%
%   Not yet wired in the MEX dispatcher (see solve.poisson).
    nxr.manifold.impl.notWired('solve.heat');
end
